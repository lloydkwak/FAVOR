nohup: 입력 무시
+ cd /home/nl/favor_project
+ SQUARE_CONTAINER_NAME_PATTERN=docker-favor-run
+ echo 'Waiting for Square training container to exit...'
Waiting for Square training container to exit...
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
+ grep -q docker-favor-run
+ docker ps --format '{{.Names}}'
+ sleep 30
+ docker ps --format '{{.Names}}'
+ grep -q docker-favor-run
+ sleep 30
